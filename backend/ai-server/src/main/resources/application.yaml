spring:
  application:
    name: haze-ai-hub
  profiles:
    active: dev
  ai:
    dashscope:
      api-key: ${ai.bailian.api-key}
      chat:
        options:
          model: deepseek-v3
          temperature: 0.8
      embedding:
        options:
          model: text-embedding-v4 # 文本向量
#          model: qwen2.5-vl-embedding # 多模态向量模型
    ai:
      vectorstore:
        redis:
          index: spring_ai_index # 向量库索引名
          initialize-schema: true # 是否初始化向量库索引结构（框架自动创建索引库结构）
          prefix: "doc:" # 向量库所有key的前缀（用于区分不同的库）
    data:
      redis:
        host: ${ai.redis.host:localhost} # redis服务地址
  datasource:
    url: jdbc:postgresql://${ai.datasource.host:localhost}:${ai.datasource.port:5432}/${ai.datasource.database:haze-ai-hub}?sslmode=disable
    driver-class-name: ${ai.datasource.driver-class-name:org.postgresql.Driver}
    username: ${ai.datasource.username:postgres}
    password: ${ai.datasource.password:1234}
logging:
  level:
    org.springframework.ai.chat.client.advisor: debug
    top.hazenix.hazeaihub: debug

mybatis-plus:
  #mapper配置文件
  mapper-locations: classpath*:mapper/**/*.xml
  type-aliases-package: top.hazenix.hazeaihub.entity # 别名扫描包(项目中实体类放置的包路径)
  configuration:
    #开启驼峰和下划线的映射
    map-underscore-to-camel-case: true
    # log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    cache-enabled: false # 是否开启二级缓存
    # 配置默认的JDBC类型映射
    jdbc-type-for-null: NULL
  global-config:
    db-config:
      id-type: AUTO # 设置全局的id生成策略
      update-strategy: not_null # 设置更新策略：只更新非空字段
  # 配置类型处理器
  type-handlers-package: top.hazenix.hazeaihub.handler
# Knife4j和SpringDoc配置
springdoc:
  api-docs:
    enabled: true
    path: /v1/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
  default-flat-param-object: false

knife4j:
  enable: true
  production: false

# Astra 知识库配置
astra:
  enabled: true
  upload:
    max-file-size: 100
    allowed-types:
      - application/pdf
      - application/vnd.openxmlformats-officedocument.wordprocessingml.document
      - image/jpeg
      - image/png
      - image/gif
      - image/webp
      - audio/mpeg
      - audio/wav
      - application/x-xmind
      - text/plain
  embedding:
    model: text-embedding-v3
    dimensions: 1024
    max-tokens: 8192
  redis-stream:
    queue-name: astra:parse:queue
    dlq-name: astra:parse:dlq
    consumer-group: astra-parse-group
    consumer-name-prefix: consumer-
    max-retries: 3
    retry-delay-ms: 5000
    block-timeout-ms: 5000
  setting:
    language: zh-cn
    enable-swagger-models: true
    enable-document-manage: true
    swagger-model-name: 实体类列表
    enable-version: false
    enable-reload-cache-parameter: false
    enable-after-script: true
    enable-filter-multipart-api-method-type: POST
    enable-filter-multipart-apis: false
    enable-request-cache: true
    enable-host: false
    enable-host-text: ""
    enable-home-custom: false
    home-custom-path: classpath:markdown/home.md
    enable-search: true
    enable-footer: true
    enable-footer-custom: false
    footer-custom-content: ""
    enable-dynamic-parameter: false
    enable-debug: true
    enable-open-api: true
    enable-group: true